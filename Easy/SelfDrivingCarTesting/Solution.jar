import java.util.*;
import java.io.*;
import java.math.*;

/**
 * Auto-generated code below aims at helping you parse
 * the standard input according to the problem statement.
 **/
class Solution {

    public static void main(String args[]) {
        Scanner in = new Scanner(System.in);
        int N = in.nextInt();
        if (in.hasNextLine()) {
            in.nextLine();
        }
        String xthenCOMMANDS[] = in.nextLine().split(";");
        
        int currentPosition = Integer.parseInt(xthenCOMMANDS[0])-1;
        int movesLeftThisPart=0;
        int numbOfSquenze=1;
        
        StringBuilder sb = new StringBuilder(xthenCOMMANDS[numbOfSquenze]);
        sb.deleteCharAt(xthenCOMMANDS[numbOfSquenze].length()-1);
        movesLeftThisPart=Integer.parseInt(sb.toString());
        
        for (int i = 0; i < N; i++) {
            String rthenROADPATTERN[] = in.nextLine().split(";");
            int length = Integer.parseInt(rthenROADPATTERN[0]);           

            for (int j=0;j<length;j++) {
            	
                // direction
            	
            	
                char ch[]=xthenCOMMANDS[numbOfSquenze].toCharArray();
                if (ch[ch.length-1]=='R') {currentPosition++;}
                else if (ch[ch.length-1]=='L') {currentPosition--;}
                
            	
            StringBuilder result =new StringBuilder(rthenROADPATTERN[1]);
            result.setCharAt(currentPosition, '#');
            System.out.println(result.toString());
            
            
            movesLeftThisPart--;
            
            if(movesLeftThisPart==0 && numbOfSquenze<xthenCOMMANDS.length-1) {
            numbOfSquenze++;
             sb = new StringBuilder(xthenCOMMANDS[numbOfSquenze]);
            sb.deleteCharAt(xthenCOMMANDS[numbOfSquenze].length()-1);
            movesLeftThisPart=Integer.parseInt(sb.toString());
            
            
            }
            
            
            }
            
            
        }

        // Write an action using System.out.println()
        // To debug: System.err.println("Debug messages...");

        
    }
}